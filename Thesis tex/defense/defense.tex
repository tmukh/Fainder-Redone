% Thesis defense, 20 minutes
% Build: latexmk -pdf defense.tex   (from Thesis tex/defense/)
\documentclass[aspectratio=169,10pt]{beamer}

\usetheme{metropolis}
\metroset{sectionpage=progressbar, numbering=fraction, progressbar=frametitle}

\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, decorations.pathreplacing}
\graphicspath{{../img/gen/}{../img/}}

\newcommand{\bcombo}{\texttt{best-combo}}
\newcommand{\good}[1]{\textcolor{green!50!black}{#1}}
\newcommand{\bad}[1]{\textcolor{red!70!black}{#1}}

\title{Hardware-Conscious Performance Engineering of Fainder for Distribution-Aware Dataset Search}
\subtitle{Master's Thesis Defense}
\author{Tarik Abu Mukh}
\institute{Technische Universit\"at Berlin \\ Chair of Database Systems and Information Management (DIMA)\\[1ex]
Reviewers: Prof.\ Dr.\ Volker Markl, Prof.\ Dr.\ Matthias B\"ohm \quad Advisor: Lennart Behme}
\date{\today}

\begin{document}

\maketitle

\begin{frame}{Outline}
  \tableofcontents[sections={1-7}]
\end{frame}

% ---------------------------------------------------------------- 1. Context
\section{Problem}

\begin{frame}{Distribution-Aware Dataset Search and Fainder}
  \begin{itemize}
    \item Raw data not searchable (privacy, licensing, scale) $\to$ per-column \textbf{histograms}
    \item Predicate class: \emph{``find all columns whose $p$-th percentile exceeds threshold $\tau$''}
    \item \textbf{Fainder} [Behme et al., VLDB 2024]: the histogram scan becomes a handful of \textbf{binary searches}
    \item GitTables: 5M histograms across $\sim$1M tables; $>$2 orders of magnitude vs profile scan, 1\,TB $\to$ 3.2\,GB
  \end{itemize}
  \vfill
  \begin{alertblock}{This thesis}
    How does Fainder's query phase run on a modern multi-core server?
  \end{alertblock}
\end{frame}

\begin{frame}{Fainder in Plain Words}
  \begin{center}
  \resizebox{\linewidth}{!}{%
  \begin{tikzpicture}[
      node distance=4mm,
      box/.style={draw, rounded corners=1pt, align=center, font=\footnotesize, inner sep=5pt, minimum height=11mm},
      arr/.style={-{Stealth[length=2mm]}, thick}
    ]
    \node[box] (a) {5{,}017{,}619 column\\summaries (histograms)};
    \node[box, right=of a] (b) {grouped into $\sim$191\\families of similar shape};
    \node[box, right=of b] (c) {one shared x-axis\\per family};
    \node[box, right=of c] (d) {at each x-axis point:\\sorted fractions $+$ IDs};
    \node[box, right=of d] (e) {a query jumps into\\two sorted lists};
    \draw[arr] (a) -- (b);
    \draw[arr] (b) -- (c);
    \draw[arr] (c) -- (d);
    \draw[arr] (d) -- (e);
  \end{tikzpicture}}
  \end{center}
  \begin{columns}[T]
    \column{0.54\textwidth}
    \centering
    {\small One cluster's x-axis (its bin edges):}\\[1.5ex]
    \begin{tikzpicture}
      \draw[thick] (0,0) -- (5.6,0);
      \foreach \x/\l in {0.2/0, 1.4/10, 2.6/20, 3.8/30, 5.4/50} {
        \draw[thick] (\x,0.12) -- (\x,-0.12) node[below, font=\footnotesize] {\l};
      }
      \draw[<-, very thick, orange!85!black] (2.6,0.2) -- (2.6,0.85) node[above, font=\footnotesize] {query: $\tau = 20$};
    \end{tikzpicture}
    \column{0.42\textwidth}
    \centering
    {\small At that tick, one sorted list:}\\[1ex]
    \footnotesize
    \begin{tabular}{ll}
      \toprule
      sorted CDFs at $\leq$20 & IDs \\
      \midrule
      $[0.25,\ 0.45,\ 0.50]$ & H2 H5 H1 \\
      \bottomrule
    \end{tabular}
  \end{columns}
  \vfill
  \begin{itemize}
    \item Per tick, one sorted list: the fraction of each column's values below that point
    \item \alert{``median $> 20$'' $\iff$ ``fraction $\leq 20$ is $< 0.5$''}: $\tau$ picks the list, $p$ picks the cut
    \item Clusters are independent $\to$ parallel. Search $=$ few dependent reads, emit $=$ many writes
  \end{itemize}
\end{frame}

\begin{frame}{The Python Baseline}
  \begin{columns}[T]
    \column{0.55\textwidth}
    \begin{itemize}
      \item GIL: one thread at a time, and \texttt{searchsorted} never releases it
      \item Thread sweep \textbf{flat}: 2\% spread at any $t$
      \item AoS layout: value--ID pairs, only values read during search $\to$ half of every line wasted
    \end{itemize}
    \column{0.42\textwidth}
    \begin{block}{10{,}000 queries, 5M histograms}
      \centering
      \Large $\sim$2.4 hours \\[0.5ex]
      \normalsize (8{,}589\,s, any thread count)
    \end{block}
  \end{columns}
  \vfill
  \begin{itemize}
    \item IPC 2.35 vs Rust's 0.96, yet $378\times$ slower: $6.2\times$ the instructions, one effective core
    \item Rust wins by executing fewer instructions across all cores in parallel, not by winning on IPC
  \end{itemize}
\end{frame}

\begin{frame}{Goal and Scope}
  \begin{itemize}
    \item Close the algorithm--implementation gap
    \item Rust query engine behind the existing Python API (PyO3), with $>$20 optimisation axes ablated
    \item Constraint: bitwise-identical results, precision $=$ recall $=$ 1.000, every build
    \item Out of scope: algorithm changes, GPU, distributed execution
  \end{itemize}
  \vfill
  \begin{alertblock}{The investigation is the thesis, the port is its instrument}
    Four hardware ceilings $+$ negative composability $+$ a pre-flight methodology.
  \end{alertblock}
\end{frame}

% ---------------------------------------------------------------- 2. Method
\section{Method}

\begin{frame}{Experimental Setup}
  \begin{itemize}
    \item 2$\times$ Xeon Platinum 8468H (Sapphire Rapids): 96 cores, 105\,MiB L3/socket, 1\,TB DDR5
    \item 48 cores per socket $\to$ $t \leq 48$ stays on one socket
  \end{itemize}
  \begin{center}
    \small
    \begin{tabular}{lrrrl}
      \toprule
      Dataset & $n_\text{hists}$ & $K_\text{eff}$ & hists/cluster & role \\
      \midrule
      \texttt{c256\_10gb}  & 324\,K  & 129 & $\sim$2.5\,K & small \\
      \texttt{c256\_30gb}  & 997\,K  & 184 & $\sim$5.4\,K & medium \\
      \texttt{c256\_56gb}  & 5.02\,M & 191 & $\sim$26\,K  & \textbf{primary} \\
      \texttt{c1024\_56gb} & 5.02\,M & 610 & $\sim$8.2\,K & OOD ($3.2\times$ \textbf{sparser}) \\
      \bottomrule
    \end{tabular}
  \end{center}
  \begin{itemize}
    \item Same 10K queries everywhere, 5-rep medians, $t \in \{1 \dots 96\}$, \texttt{perf} counters per cell
    \item 2{,}658 runs, every claim traceable to a benchmark-database row
  \end{itemize}
\end{frame}

\begin{frame}{The Rust Engine}
  \begin{itemize}
    \item PyO3 extension module; clustering and loading stay in Python
    \item Rayon work-stealing over clusters (idle threads take work); no GIL in the hot path
    \item One Cargo feature flag per axis $\to$ every variant is a separate measurable build:
    \begin{itemize}
      \item layout: SoA / AoS, f16, packed IDs \quad search: AVX-512, 8-way batch, lockstep, PGM
      \item scheduling: column-centric, query-batch, morsel, pinning \quad memory: pooled, mimalloc, NUMA
    \end{itemize}
  \end{itemize}
\end{frame}

% ---------------------------------------------------------------- 3. Results
\section{Results: Four Ceilings}

\begin{frame}{Finding 1: Four Performance Ceilings}
  \begin{columns}[T]
    \column{0.60\textwidth}
    \includegraphics[width=\linewidth]{ch4_four_ceilings.pdf}
    \column{0.38\textwidth}
    \small
    \begin{enumerate}
      \item Single reads ($t \leq 8$): \bad{refuted}
      \item Shared L3 ($t \approx 16$)
      \item Memory-traffic pressure ($t \in [32,48]$)
      \item Cross-socket link ($t > 48$)
    \end{enumerate}
    \vfill
    % Ceiling $=$ the hardware limit that binds wall time in a thread range --- what you would have to fix to go faster there. More threads don't raise it; only the right intervention class does.
  \end{columns}
\end{frame}

\begin{frame}{Ceiling (i): Single-Thread Latency, Refuted}
  \begin{center}
  \resizebox{0.9\linewidth}{!}{%
  \begin{tikzpicture}[
      probe/.style={draw, rounded corners=1pt, font=\footnotesize, inner sep=4pt, minimum height=6mm},
      arr/.style={-{Stealth[length=2mm]}, thick}]
    \node[probe] (p1) {probe 1};
    \node[probe, right=6mm of p1] (p2) {probe 2};
    \node[probe, right=6mm of p2] (p3) {probe 3};
    \node[right=4mm of p3] (d) {$\cdots$};
    \node[probe, right=4mm of d] (p5) {probe 21};
    \node[probe, right=6mm of p5] (p6) {probe 22};
    \draw[arr] (p1) -- node[below=1pt, font=\scriptsize] {waits} (p2);
    \draw[arr] (p2) -- node[below=1pt, font=\scriptsize] {waits} (p3);
    \draw[arr] (p3) -- (d);
    \draw[arr] (d) -- (p5);
    \draw[arr] (p5) -- (p6);
    \draw[arr, orange!85!black, dashed] (p1.north) to[bend left=22] node[above, font=\scriptsize] {\texttt{pgm}: predict the landing, skip the chain} (p5.north);
    \draw[orange!85!black, decorate, decoration={brace, mirror, amplitude=4pt}] ([yshift=-2.5mm]p5.south west) -- ([yshift=-2.5mm]p6.south east) node[midway, below=5pt, font=\scriptsize] {\texttt{simd}: last 16 values, one compare};
  \end{tikzpicture}}
  \end{center}
  \begin{center}
    \footnotesize
    \begin{tabular}{llr}
      \toprule
      Ablation & Idea & vs default, all 18 cells \\
      \midrule
      \texttt{simd} & vectorise the last $\sim$16 steps & $0.90$--$1.07\times$ \; noise \\
      \texttt{batch-search} & overlap 8 searches' loads & $0.96$--$1.09\times$ \; noise \\
      \texttt{horizontal-simd} & 16 queries walk in lockstep & $0.95$--$1.12\times$ \; noise \\
      \texttt{pgm} & predict the position, skip the chain & $0.91$--$1.13\times$ \; noise \\
      \bottomrule
    \end{tabular}
  \end{center}
  \begin{itemize}
    \item All four: null. The wall is the \emph{number} of chains, not their depth: 10\,K queries $\times$ 191 clusters $\times$ 2 searches $\approx$ 3.8\,M chains of $\sim$22 cheap, cache-resident probes
  \end{itemize}
\end{frame}

\begin{frame}{Ceiling (ii): Shared L3 Capacity}
  Claim: at $t \in [8,32]$, the threads' combined working sets overflow the shared 105\,MiB L3.\\
  {\footnotesize Two probes (ratio vs default on the primary workload, $<1$ = faster):}
  \begin{center}
    \footnotesize
    \begin{tabular}{lrrrrrr}
      \toprule
      Build & $t{=}1$ & $t{=}8$ & $t{=}16$ & $t{=}32$ & $t{=}64$ & $t{=}96$ \\
      \midrule
      \texttt{aos} (capacity probe: doubles bytes/line) & $1.03$ & \bad{$1.17$} & $1.02$ & $1.00$ & \good{$0.96$} & $0.99$ \\
      \texttt{pooled} (allocator probe: no per-cluster allocs) & \good{$0.53$} & \good{$0.66$} & \good{$0.80$} & $0.95$ & $0.93$ & $1.02$ \\
      \bottomrule
    \end{tabular}
  \end{center}
  \begin{center}
  \resizebox{0.96\linewidth}{!}{%
  \begin{tikzpicture}[
      th/.style={draw, rounded corners=1pt, font=\scriptsize, inner sep=2.5pt},
      arr/.style={-{Stealth[length=1.8mm]}, thick}]
    % ---- dense panel: queue at the lock
    \node[font=\scriptsize\bfseries] at (2.1,1.6) {dense: queue at the lock};
    \node[th] (t1) at (0,0.95) {T1};
    \node[th] (t2) at (0,0.45) {T2};
    \node[th] (t3) at (0,-0.05) {T3};
    \node[draw, align=center, font=\scriptsize] (lock) at (2.1,0.45) {allocator\\lock};
    \node[draw, font=\scriptsize] (buf) at (4.2,0.45) {buffer};
    \draw[arr] (t1.east) -- (lock.west);
    \draw[arr] (t2.east) -- node[above, font=\tiny] {wait} (lock.west);
    \draw[arr] (t3.east) -- (lock.west);
    \draw[arr] (lock.east) -- node[above, font=\tiny] {one at a time} (buf.west);
    \node[font=\scriptsize] at (2.1,-0.85) {fix: \texttt{pooled} (one buffer per query, queue gone)};
    % ---- sparse panel: churn through the L3
    \node[font=\scriptsize\bfseries] at (9.0,1.6) {sparse: churn through the shared L3};
    \draw[thick] (6.8,-0.2) rectangle (10.6,1.1);
    \node[font=\tiny] at (8.7,0.92) {shared L3 (105\,MiB)};
    \foreach \x/\y in {7.05/0.55, 7.5/0.55, 7.95/0.55, 8.4/0.55, 8.85/0.55, 9.3/0.55, 9.75/0.55, 10.2/0.55, 7.05/0.08, 7.5/0.08, 7.95/0.08, 8.4/0.08, 8.85/0.08, 9.3/0.08}
      \fill[orange!60] (\x,\y) rectangle ++(0.3,0.3);
    \node[draw, font=\scriptsize] (ram) at (12.1,0.45) {RAM};
    \draw[arr] (10.6,0.7) -- node[above, font=\tiny] {evicted} (ram.170);
    \draw[arr] (ram.190) -- node[below, font=\tiny] {refetched} (10.6,0.2);
    \node[font=\scriptsize] at (9.4,-0.85) {fix: \texttt{f16} (half the bytes, twice as much stays resident)};
  \end{tikzpicture}}
  \end{center}
  Two culprits, one thread range: the \textbf{lock} on dense data, the \textbf{cache} on sparse. The data's shape decides which binds.
\end{frame}

\begin{frame}{Ceiling (iii): On-Socket Memory-Traffic Pressure}
  \begin{center}
  \begin{tikzpicture}[
      core/.style={draw, rounded corners=1pt, font=\scriptsize, inner sep=2.5pt},
      arr/.style={-{Stealth[length=1.8mm]}, thick}]
    \node[core] (c1) at (0,1.0) {C1};
    \node[core] (c2) at (0,0.62) {C2};
    \node at (0,0.3) {$\vdots$};
    \node[core] (c4) at (0,-0.1) {C48};
    \node[draw, align=center, font=\scriptsize] (q) at (2.9,0.45) {shared queues\\(L3 $\to$ mesh $\to$ controller)};
    \node[draw, font=\scriptsize] (ram) at (6.2,0.45) {RAM};
    \draw[arr] (c1.east) -- (q.west);
    \draw[arr] (c2.east) -- node[above, font=\tiny] {wait} (q.west);
    \draw[arr] (c4.east) -- (q.west);
    \draw[arr] (q.east) -- node[above, font=\tiny] {fixed service rate} (ram.west);
  \end{tikzpicture}
  \end{center}
  One road, more requesters $\to$ longer queues, at $\sim$0.5\% of STREAM peak: \textbf{congestion, not saturation}.
  \smallskip

  {\footnotesize The signature, \texttt{c256\_30gb}, wall in seconds:}
  \begin{center}
    \small
    \begin{tabular}{lrrr}
      \toprule
      Build & $t{=}64$ & $t{=}96$ & step \\
      \midrule
      default & $4.15$ & $5.89$ & \bad{$+42\%$} \quad (IPC $1.93 \to 0.55$, LLC misses $\times 1.6$) \\
      \texttt{packed-ids} & $3.88$ & $4.70$ & \bad{$+21\%$} \quad (same step, half the damage) \\
      \bottomrule
    \end{tabular}
  \end{center}
  \begin{itemize}
    \item Anti-scaling: 32 added cores, $42\%$ \emph{slower}. Fix $=$ fewer bytes: \texttt{packed-ids} (20-bit IDs, $-37\%$ emit bytes) $\to$ \good{$-20\%$} at the worst cell
  \end{itemize}
\end{frame}

\begin{frame}{Ceiling (iv): Cross-Socket UPI Interconnect}
  Same workload, four memory placements (\texttt{numactl} probe, wall in seconds):
  \begin{center}
    \small
    \begin{tabular}{llrrrr}
      \toprule
      Placement & meaning & $t{=}32$ & $t{=}48$ & $t{=}64$ & $t{=}96$ \\
      \midrule
      A: unpinned & OS default, first-touch & $19.28$ & $16.97$ & $18.19$ & $17.58$ \\
      B: single socket & cores $+$ memory on socket 0 & \good{$\mathbf{16.77}$} & \good{$\mathbf{15.04}$} & -- & -- \\
      C: interleave & pages alternate sockets per 4\,KB & \bad{$25.87$} & \bad{$23.73$} & \bad{$22.91$} & \bad{$24.83$} \\
      D: cross-socket & data socket 0, threads socket 1 (all remote) & $19.08$ & $16.86$ & -- & -- \\
      \bottomrule
    \end{tabular}
  \end{center}
  \begin{itemize}
    \item B vs A: removing all cross-socket traffic saves \good{11--13\%}: that saving \emph{is} the link cost
    \item C vs A: ``fair'' page balancing costs \bad{26--41\%}: half of all accesses land remote, and the per-page alternation defeats the prefetcher
    \item D $\approx$ A: even the worst case matches default $\to$ the baseline was already paying the link
  \end{itemize}
\end{frame}

% ---------------------------------------------------- 4. Structural finding
\section{Results: Negative Composability}

\begin{frame}{Finding 2: Optimisations Compose Negatively}
  \bcombo{} $=$ \texttt{pooled f16 simd pin-cores cluster-prefetch mimalloc} (each regime-best somewhere), $0.773\times$ default wall at $t{=}16$.\\
  {\footnotesize Layering one more optimisation on top:}
  \begin{center}
    \footnotesize
    \begin{tabular}{llr}
      \toprule
      Composition & Intended axis & Outcome \\
      \midrule
      \bcombo{} $+$ \texttt{packed-ids} & emit-ID bandwidth & \bad{NEG} 12/18 cells, up to $+24\%$ \\
      \bcombo{} $+$ qbatch $K{=}128$ & cold-load amortisation & \bad{NEG} every cell, $+8$ to $+32\%$ \\
      qbatch $+$ morsel & cross-worker coordination & \bad{NEG} every cell, $+8$ to $+12\%$ \\
      \texttt{packed-ids} $+$ \texttt{local-ids} & read-side bandwidth & \bad{NEG} $+186$ to $+245\%$ \\
      first-touch $+$ interleave & memory-locality balance & \bad{NEG} every cell, $+26$ to $+41\%$ \\
      \bottomrule
    \end{tabular}
  \end{center}
  {\footnotesize qbatch: batch $K$ queries per cluster visit \quad morsel: cross-worker coordination \quad \texttt{local-ids}: per-cluster renumbering}
  \begin{itemize}
    \item The coarser optimisation absorbed the headroom; the finer one's overhead turns net cost
    \item All five plausible arguments falsified by \texttt{perf}. Five axes, one shape: structural, not coincidence
  \end{itemize}
\end{frame}

\begin{frame}{Finding 3: Pre-Flight Ceiling Identification}
  Classical ablation: build $\to$ measure vs default $\to$ keep if faster. Assumes independence. The last slide falsified that five times.
  \vfill
  Before writing code:
  \begin{enumerate}
    \item Probe the binding ceiling at the target composition (\texttt{perf})
    \item Axis no longer binds there? Stop
    \item Predict the saving (bandwidth-budget arithmetic)
    \item Measure where the ceiling binds, separating saving from overhead
    \item A negative composition counts as a result
  \end{enumerate}
  \vfill
  Machine-agnostic: applies to any hardware-near system with stacked ceilings.
\end{frame}

\begin{frame}{Dispatch on Regime}
  \begin{itemize}
    \item No dominating build $\to$ per-regime selection: $(n_\text{hists}, t) \to$ build $+$ flags
    \item \bcombo{} at moderate $t$ / big data; \texttt{packed-ids} at $t{=}64$; qbatch at $t{=}96$; columnar at low $t$; default on small data
    \item In-distribution: \good{17/18} exact, \good{18/18} within 5\%, and \good{23/24} combined with OOD
  \end{itemize}
  \smallskip
  Out-of-distribution validation (\texttt{c1024\_56gb}, $3.2\times$ sparser, never tuned on):
  \begin{center}
    \footnotesize
    \begin{tabular}{rllr}
      \toprule
      $t$ & policy pick & regime-best & gap \\
      \midrule
      1 & qbatch $K{=}64$ & qbatch $K{=}128$ & \good{$+0.9\%$} \\
      8--32 & \bcombo{} & \bcombo{} & \good{$0.0\%$} \\
      64 & \texttt{packed-ids} & \bcombo{} & \good{$+0.2\%$} \\
      96 & qbatch $K{=}128$ & qbatch $K{=}128$ & \good{$0.0\%$} \\
      \bottomrule
    \end{tabular}
  \end{center}
  \smallskip
  Boundaries are machine-specific. The probing procedure is not.
\end{frame}

% ---------------------------------------------------------------- 5. Speedup
\section{Results: Comparison with Python}

\begin{frame}{Speedup over the Python Baseline}
  \begin{columns}[T]
    \column{0.50\textwidth}
    \includegraphics[width=\linewidth]{ch4_e2e_speedup.pdf}
    \column{0.48\textwidth}
    \footnotesize
    \begin{tabular}{lrr}
      \toprule
      Workload & search & end-to-end \\
      \midrule
      \texttt{c256\_10gb} & $22.7\times$ & $598\times$ \\
      \texttt{c256\_30gb} & $4.6\times$  & $583\times$ \\
      \texttt{c256\_56gb} & $1.34\times$ & $530\times$ \\
      \bottomrule
    \end{tabular}
    \begin{itemize}
      \item End-to-end: 2.4\,h $\to$ 16\,s
      \item $\sim$99\% of the ratio $=$ avoiding Python's result-dict construction
      \item Search alone bends toward the shared ceilings
      \item Rebinning kernel: $4.7\times$ / $2.2\times$ on construction
    \end{itemize}
  \end{columns}
\end{frame}

% ---------------------------------------------------------------- 6. Related work
\section{Related Work}

\begin{frame}{Positioning Against the State of the Art}
  \begin{itemize}
    \item \textbf{Layout: adopted.} Manegold et al.\ [2000]: memory access dominates in-memory work $\to$ our SoA layout
    \item \textbf{Search: refuted.} FAST [Kim 2010], Eytzinger [Khuong, Morin 2017], $k$-ary [Schlegel 2009], PGM [Ferragina 2020]: all assume latency-bound search. Ceiling (i) says no on this workload
    \item \textbf{Execution: followed.} Neumann [2011]: compile, don't interpret $\to$ the port itself; morsel-driven [Leis 2014] $\to$ work-stealing
    \item \textbf{Composition: challenged.} Selinger [1979], Caladan [2020], memory-wall [Wulf, McKee 1995]: all assume gains compose $\to$ five mechanism-grounded counterexamples $+$ a falsifiable pre-flight check
  \end{itemize}
  \vfill
  Gap filled: the first hardware characterisation of histogram-based percentile search, and the first horizontal-SIMD measurement on Sapphire Rapids.
\end{frame}

% ---------------------------------------------------------------- 7. Wrap-up
\section{Conclusion}

\begin{frame}{Contributions}
  \begin{enumerate}
    \item Rust re-implementation: bitwise-identical, $530$--$598\times$ end-to-end, PyO3-integrated \hfill \emph{engineering}
    \item Four-ceiling characterisation on Sapphire Rapids, via \texttt{perf} \hfill \emph{scientific}
    \item Negative-composability pattern: five instances, five axes \hfill \emph{scientific}
    \item Pre-flight ceiling identification \hfill \emph{methodological}
    \item Dispatch-on-regime policy, 23/24 within 5\% incl.\ OOD \hfill \emph{engineering}
  \end{enumerate}
  \vfill
  (1) and (5) are the staging ground. (2)--(4) generalise.
\end{frame}

\begin{frame}{Future Work}
  \begin{itemize}
    \item NUMA-aware build for $t > 48$: shard clusters across sockets, node-local workers
    \item Adaptive precision: f32 at low $t$, f16 at high $t$ / sparse density, selected at runtime
    \item Dispatch validation beyond the $\sim$8.2\,K--26.3\,K hists/cluster range
  \end{itemize}
\end{frame}

\begin{frame}{Closing Remark}
  \begin{itemize}
    \item The speedups are specific to this codebase, machine, and workload
    \item What generalises: whether optimisation $B$ helps depends on which ceiling $A$ already collected, which per-operator cost models cannot see
  \end{itemize}
  \vfill
  \begin{alertblock}{Closing remark}
    Identify the binding ceiling at the target composition before committing the implementation cost of the next optimisation.
  \end{alertblock}
\end{frame}

\begin{frame}[standout]
  Questions?
\end{frame}

% ================================================================ BACKUP
\appendix
\section{Backup}

\begin{frame}{Backup: One Query, End to End}
  Query: \emph{``median $>$ 20''} $=$ $(p{=}0.5,\; >,\; \tau{=}20)$
  \hfill \alert{median $> 20 \iff$ CDF$(20) < 0.5$}
  \vspace{1ex}
  \begin{columns}[T]
    \column{0.46\textwidth}
    \centering
    Cluster A index (toy: 3 histograms)\\[0.5ex]
    \footnotesize
    bin edges: $[0, 10, \mathbf{20}, 30, 50]$\\[1ex]
    \begin{tabular}{lll}
      \toprule
      edge & sorted CDFs & IDs \\
      \midrule
      $\leq$10 & $[0.00, 0.10, 0.20]$ & H2 H1 H5 \\
      $\boldsymbol{\leq}$\textbf{20} & $[\mathbf{0.25}, \mathbf{0.45}, 0.50]$ & \textbf{H2 H5} H1 \\
      $\leq$30 & $[0.75, 0.75, 0.80]$ & H2 H5 H1 \\
      \bottomrule
    \end{tabular}
    \column{0.50\textwidth}
    \begin{enumerate}
      \item Binary search $\tau{=}20$ in the bin edges $\to$ column ``$\leq$20''
      \item Binary search $0.5$ in that sorted column $\to$ cut after 2 entries
      \item Emit the IDs before the cut: \good{H2, H5} \\ {\footnotesize(H1 sits exactly at $0.50$: median $= 20$, not $> 20$)}
      \item Cluster B (salaries, range $\geq$ 10\,000): $\tau$ below range $\to$ \textbf{all match, no search}
      \item Union across clusters $=$ the answer
    \end{enumerate}
  \end{columns}
  \vfill
  Clusters are independent $\to$ the cluster loop parallelises. Search $=$ few dependent reads, emit $=$ many writes.
\end{frame}

\begin{frame}{Backup: Why is end-to-end $530\times$ if search is only $1.34\times$?}
  \begin{itemize}
    \item Python's per-(query, cluster) result-dict construction dominates its wall. Measured at $t{=}16$, materialisation accounts for:
    \begin{itemize}
      \item \texttt{c256\_10gb}: $580.9$\,s of $599.7$\,s ($96.9\%$)
      \item \texttt{c256\_30gb}: $1{,}878.5$\,s of $1{,}890.3$\,s ($99.4\%$)
      \item \texttt{c256\_56gb}: $8{,}568$\,s of $8{,}589$\,s ($99.8\%$)
    \end{itemize}
    \item Rust emits results without per-cluster dict construction, so it avoids this cost entirely.
    \item We therefore report both columns: suppress-vs-suppress isolates the search engines, with-results-vs-with-results is what a user experiences.
    \item Conflating the two would attribute Python's materialisation cost to Rust's search engineering.
  \end{itemize}
\end{frame}

\begin{frame}{Backup: Why does f16 win nothing standalone?}
  \begin{itemize}
    \item Prediction: halve the footprint, win in the regime where the f32 working set exceeds L3 but the f16 one fits.
    \item On \texttt{c256\_56gb} the per-cluster f32 slice ($\sim$105\,KiB) is already L2-resident. Halving it unlocks no new cache-resident regime, while every comparison pays the f16$\to$f32 dequantisation.
    \item Result: no significant standalone win in the 10-rep fine-$t$ sweep, and a $+15.6\%$ drag inside the bundle at $t{=}16$.
    \item On \texttt{c1024\_56gb} ($3.2\times$ sparser, $\sim$33\,KiB slices, 610 clusters) the aggregate cluster-loop footprint binds. There f16 carries $\sim$20\% of the bundle win.
    \item Footprint optimisations are density-conditional.
  \end{itemize}
\end{frame}

\begin{frame}{Backup: Why ``memory-traffic pressure'' and not ``DRAM bandwidth''?}
  \begin{itemize}
    \item STREAM Triad on this machine reaches $\sim$330\,GB/s aggregate.
    \item Accounting the workload's LLC-miss traffic at $64$\,B per missed probe puts it near $0.5\%$ of that peak.
    \item ``Bandwidth-bound'' claims saturation of the DRAM channels. The counters show nothing close to saturation.
    \item What we observe is anti-scaling from aggregate pressure on the on-socket memory subsystem: queue occupancy and allocator traffic, at utilisation far below peak.
    \item The name matters because it predicts which interventions help: fewer bytes per comparison and per emit. A pure bandwidth upgrade would not.
  \end{itemize}
\end{frame}

\begin{frame}{Backup: The local-ids regression}
  \begin{itemize}
    \item \texttt{local-ids} (per-cluster reindexing) was pre-registered as a positive prediction: narrower IDs, less read-side bandwidth.
    \item Measured: \bad{$+186$ to $+245\%$} at $t \in [8, 64]$ on top of \texttt{packed-ids}. The prediction is falsified.
    \item Mechanism, from a direct \texttt{perf stat} probe: the bw$\approx$13 decoder's access pattern over the packed buffer raises the dTLB miss rate $52\times$ per instruction.
    \item TLB-walk stalls at $\sim$100 cycles each drown the bandwidth saving.
    \item Future work (F1): Transparent Huge Pages (2\,MiB pages) cut TLB-miss frequency $512\times$ per covered page. That probe is the natural follow-up.
  \end{itemize}
\end{frame}

\begin{frame}{Backup: What generalises beyond this machine?}
  \begin{itemize}
    \item The thresholds do not generalise. The methodology does.
    \item All experiments ran on one Sapphire Rapids 8468H configuration. Genoa, Granite Rapids, or Grace would shift per-ceiling thresholds and dispatch boundaries.
    \item The pre-flight procedure (probe the binding ceiling at the target composition) is machine-agnostic. The four-ceiling taxonomy is a hypothesis set to re-test on new hardware, and not a constant.
    \item Density scope: dispatch is validated between $\sim$8{,}200 and $\sim$26{,}300 hists/cluster. Outside that range the policy is a prediction.
    \item A multi-machine deployment would add a fifth candidate ceiling (network) with its own intervention class.
  \end{itemize}
\end{frame}

\begin{frame}{Backup: How was correctness validated?}
  \begin{itemize}
    \item Every Rust build validates bitwise-identical against the Python reference across all workloads: precision $=$ recall $=$ 1.000.
    \item Validation runs inside the benchmark harness, so each measured configuration is also a correctness check. Suppress mode is a stopwatch setting only. Correctness is always checked with results on.
    \item Rebinning kernel: verified against the Python reference on 200 queries. Rust rounds half-away, NumPy uses banker's rounding. The maximum cumulative difference is $10^{-4}$ at a handful of rows, with no effect on sort order or query results.
    \item The kernel falls back to the Python pipeline if the extension is not importable or for \texttt{cubic\_spline} bin estimation.
  \end{itemize}
\end{frame}

\begin{frame}{Backup: Why Rust, and not C++, Cython, or Numba?}
  \begin{itemize}
    \item The requirements: no GIL in the hot path, explicit control over layout and allocation, safe shared-memory parallelism, and a maintainable Python integration.
    \item Rust with PyO3 covers all four: Rayon work-stealing without data races, Cargo feature flags that make every ablation a separate reproducible build, and a drop-in extension module behind the existing API.
    \item C++ could reach the same performance. The feature-flag ablation matrix and memory safety under 96-thread parallelism are where Rust paid off in practice.
    \item Numba and Cython remove less of the interpreter overhead and leave allocator and layout decisions with Python. Our measurements show those layers are the actual cost.
  \end{itemize}
\end{frame}

\begin{frame}{Backup: Why does the rebinning speedup drop at 5M histograms?}
  \begin{itemize}
    \item $4.72\times$ (324\,K) and $4.77\times$ (997\,K) drop to $2.18\times$ at 5.02\,M histograms.
    \item Cause: the kernel's own parallelism ceiling. At that scale it runs at $\sim$2 effective cores of 96, with $\sim$46\% of kernel time on a shared per-cluster allocation.
    \item An earlier hypothesis (NumPy bulk operations dominating on the Python side) was tested and refuted.
    \item This is a construction-time cost, paid once per index build. The query-phase findings are unaffected.
  \end{itemize}
\end{frame}

\end{document}
